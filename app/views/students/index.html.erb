<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Student</title>
  </head>
  <body>
    <% @students.each do |student| %>
      <p><%= link_to student.name, student_path(student) %></p>
      <p><%= link_to 'Delete', student_path(student), method: :delete %>
    <% end %>
  </body>
</html>
